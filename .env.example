# === Deploy (prod) ===
DOMAIN=tg.knclb.ru
URL_PREFIX=CHANGE_ME_openssl_rand_hex_12
DB_PASSWORD=CHANGE_ME
CERTBOT_EMAIL=CHANGE_ME@example.com
FILM_BOT_COMPOSE=/path/to/film_bot/docker-compose.yml

# === Server runtime ===
# В prod значения берёт docker-compose из переменных выше (DB_PASSWORD).
# В dev (npm run dev) server читает этот файл напрямую через dotenv-cli.
DATABASE_URL=postgresql://tg:tg_dev_password@localhost:15432/tainted_grail
BOT_TOKEN=your_telegram_bot_token
PORT=3000
ADMIN_TELEGRAM_ID=
